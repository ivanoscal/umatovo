# 🔵 Circle Counter

Приложение для Android, которое распознаёт и подсчитывает круглые объекты на фотографиях с помощью компьютерного зрения (OpenCV).

## 📱 Установка на Android

### Скачать APK

1. Перейдите в раздел [Releases](../../releases) этого репозитория
2. Скачайте последнюю версию `circle_counter.apk`
3. Откройте скачанный файл на телефоне
4. Разрешите установку из неизвестных источников (если потребуется)
5. Установите приложение

### Разрешения

Приложению требуются следующие разрешения:
- **Камера** — для съёмки фотографий
- **Хранилище** — для доступа к галерее

## 🚀 Функции

- 📷 Съёмка фото с камеры в реальном времени
- 🖼 Выбор изображения из галереи
- 🔍 Автоматическое распознавание круглых объектов
- 📊 Подсчёт количества найденных кругов
- ✅ Визуальная отметка найденных объектов на изображении

## 🛠 Сборка из исходного кода

### Требования

- Python 3.8+
- Linux (для Buildozer) или Docker на macOS/Windows
- Android SDK/NDK (устанавливаются автоматически)

### Локальный запуск (для тестирования)

```bash
# Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/umatovo.git
cd umatovo

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или: venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Запустите приложение
python main.py
```

### Сборка APK на Linux

```bash
# Установите Buildozer
pip install buildozer

# Установите зависимости системы (Ubuntu/Debian)
sudo apt-get update
sudo apt-get install -y \
    python3-pip \
    build-essential \
    git \
    python3 \
    python3-dev \
    ffmpeg \
    libsdl2-dev \
    libsdl2-image-dev \
    libsdl2-mixer-dev \
    libsdl2-ttf-dev \
    libportmidi-dev \
    libswscale-dev \
    libavformat-dev \
    libavcodec-dev \
    zlib1g-dev \
    libgstreamer1.0 \
    gstreamer1.0-plugins-base \
    gstreamer1.0-plugins-good \
    libunwind-dev \
    autoconf \
    automake \
    libtool \
    pkg-config \
    libncurses5-dev \
    libncursesw5-dev \
    libtinfo5 \
    cmake \
    libffi-dev \
    libssl-dev \
    openjdk-17-jdk

# Соберите APK
buildozer android debug
```

### Сборка APK с помощью Docker (macOS/Windows)

```bash
# Установите Docker Desktop
# https://www.docker.com/products/docker-desktop

# Используйте официальный Docker образ Buildozer
docker run --rm -v $(pwd):/home/user/hostcwd kivy/buildozer android debug
```

### Сборка через GitHub Actions

APK также автоматически собирается при push в репозиторий через GitHub Actions.
Собранный APK можно скачать в разделе Actions → последний workflow → Artifacts.

## 📁 Структура проекта

```
umatovo/
├── main.py              # Главный файл Kivy приложения
├── circle_detector.py   # Модуль OpenCV для распознавания кругов
├── buildozer.spec       # Конфигурация сборки APK
├── requirements.txt     # Зависимости Python
├── README.md            # Этот файл
└── .gitignore           # Игнорируемые файлы Git
```

## 🔧 Как это работает

1. **Загрузка изображения** — пользователь делает фото или выбирает из галереи
2. **Предобработка** — конвертация в grayscale и применение размытия
3. **Детектирование** — алгоритм Hough Circle Transform находит круги
4. **Визуализация** — найденные круги отмечаются на изображении
5. **Результат** — отображается количество найденных объектов

## ⚙️ Настройка параметров детектора

Параметры детектирования можно изменить в файле `circle_detector.py`:

```python
detector = CircleDetector(
    dp=1.2,          # Разрешение аккумулятора
    min_dist=30,     # Мин. расстояние между центрами
    param1=50,       # Порог Canny
    param2=30,       # Порог аккумулятора
    min_radius=10,   # Мин. радиус круга
    max_radius=200   # Макс. радиус круга
)
```

## 📝 Лицензия

MIT License

## 🤝 Вклад

Pull requests приветствуются! Для крупных изменений сначала откройте issue.

